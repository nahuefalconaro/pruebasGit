import { DocumentTransformPipe } from './document-transform.pipe';

describe('DocumentTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
